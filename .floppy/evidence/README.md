# Evidence

Store read-only or generated evidence records for authorized source-development
work here only when a future active section permits them.

This directory currently contains no implementation evidence because FS-01 has
not started.
