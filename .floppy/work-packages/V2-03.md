# Floppy V2-03 Accepted Work Package

**Work package:** `V2-03`  
**Title:** Accepted-State Continuity Protection  
**Status:** `IMPLEMENTATION_IN_PROGRESS`  
**Administrator:** Alva Roberts  
**Decision date:** 2026-08-10  
**Controlling roadmap revision:** `R1`  
**Preparation base checkpoint:** `2220f8c50641b87a212e6ed39b38b4d704d3c2fd`  
**Preparation base tree:** `c6b9114cbbcb0d8ac24345a97a1f9150cadaf0f0`  
**R0 / W1 acceptance base checkpoint:** `6ad7c67fdd7f22732681c193694ac3a13f0d9ea0`  
**R0 / W1 acceptance base tree:** `ca483d6cf2c097824289f02818c0ab4196e960e5`  
**Dependency:** `V2-01 CLOSED`  
**Prerequisite closed package:** `V2-02 CLOSED`  
**Implementation authority:** `V2_03_IMPLEMENTATION`  
**Repository writer:** `V2_03_WORKING_MODEL`

## 1. Administrator decision

The administrator accepts the V2-03 Accepted-State Continuity Protection work-package proposal as the V2-03 planning baseline under controlling Project Plan Revision R1.

This applies only `TR-002-ACCEPT-WORK-PACKAGE` and results in `LC-WORK-PACKAGE-ACCEPTED-NO-ACTIVE-WORK`.

This acceptance does not authorize implementation, create `V2_03_IMPLEMENTATION`, create `V2_03_WORKING_MODEL`, create a repository writer, modify `main`, migrate, integrate, merge, tag, release, or begin V2-04/V2-05 work.

## 2. Purpose and governing principle

V2-03 makes accepted project state durable against conversational context loss, fresh or replacement AI conversations, model replacement, accidental reconstruction, silent project-goal drift, silent accepted-scope drift, and historical accepted-state replacement.

**Context loss is not authority to reconstruct accepted work.**

R1 continuity intent remains: preserve the original project goal and remain as close to accepted scope as practical unless a real, explicit project-level change is required and lawfully accepted.

## 3. Accepted architecture

V2-03 owns one durable accepted-state record family.

Canonical runtime record: `.floppy/accepted-state.json`

New reusable-product schema: `schemas/bce/2.0.0/bce-accepted-state.schema.json`

Normative specification: `specs/accepted-state-continuity.md`

The accepted design requires immutable ORIGINAL accepted state, append-only accepted revision history, deterministic ORIGINAL/CURRENT_ACCEPTED/SUPERSEDED_BUT_HISTORICAL semantics, deterministic historical-rewrite and silent-drift detection, lawful explicit administrator-approved supersession, accepted-plan binding, and authority isolation.

The accepted-state record grants no implementation authority, repository writer, migration authority, integration authority, or release authority. Operational lifecycle authority remains separate.

No V1 schema modification, V2 compatibility-profile schema modification, `project-seed/.floppy/*` modification, V2-04 runtime implementation, or V2-05 Official Project Plan implementation is part of V2-03.

## 4. Frozen project_id semantics

`project_id` is a random UUIDv4 in lowercase canonical hyphenated representation, created exactly once when protected accepted state is formally established and immutable thereafter.

For new projects it is created with the accepted onboarding operation that first establishes `.floppy/accepted-state.json`. For adopted non-Floppy projects it is created when formal adoption and project-origin acceptance are recorded. Older Floppy projects predating V2-03 do not receive a fabricated or reconstructed ID merely because a new model or conversation opens the project.

It must not be derived from project name, repository URL, filesystem path, Git commit, Git branch, administrator identity, AI provider, conversation identifier, accepted scope, or accepted project outcome. Repository moves/renames, continuity-preserving forks, orchestrator/model replacement, and lawful project revisions do not change it.

## 5. Frozen protected_state_sha256 semantics

The digest covers only the parsed `protected_state` JSON object and excludes the complete accepted-state file and the digest field itself.

Canonical bytes are exactly equivalent to:

```python
json.dumps(protected_state, ensure_ascii=False, sort_keys=True, separators=(",", ":"), allow_nan=False).encode("utf-8")
```

The stored value is lowercase hexadecimal SHA-256 of those bytes. Rules: recursively deterministic key ordering; no insignificant whitespace; UTF-8; no BOM; no trailing newline in hashed bytes; direct Unicode serialization; JSON native number/boolean/null types preserved; NaN and Infinity invalid; checkout CRLF/LF outside parsed string values cannot affect the digest; characters inside JSON strings are not silently normalized.

## 6. Frozen accepted-state presence rules

Before activation, no activation registration plus no `.floppy/accepted-state.json` is valid legacy/pre-acceptance state.

After activation: ACTIVE plus missing record fails; record present plus activation registration absent fails; ACTIVE plus record present requires schema, project ID, canonical hash, binding, revision-history, and authority-isolation validation.

Older already-accepted Floppy projects that never adopted V2-03 may legitimately lack the record. The validator must never fabricate a record or project ID. After committed activation, later removal of the registration or record is continuity removal/drift and cannot be reinterpreted as legacy absence.

## 7. Accepted revision model

ORIGINAL is immutable. Administrator-accepted project-level revisions append to history. CURRENT_ACCEPTED is deterministic. Prior accepted revisions remain SUPERSEDED_BUT_HISTORICAL. Ordinary operational change cannot silently mutate protected state.

At minimum, deterministic failures distinguish `ACCEPTED_STATE_HISTORY_REWRITE` from `ACCEPTED_STATE_SILENT_DRIFT`. Detection is structure/digest/checkpoint based and does not require model judgment.

## 8. Frozen future reusable-product scope

A later separately authorized V2-03 implementation may modify exactly these 11 reusable-product paths:

1. `schemas/bce/2.0.0/bce-accepted-state.schema.json`
2. `specs/accepted-state-continuity.md`
3. `tests/test_accepted_state_continuity.py`
4. `onboarding/Floppy_1E.md`
5. `protocols/01-new-project-onboarding.md`
6. `protocols/05-revision-application.md`
7. `system-manifest.json`
8. `tools/validate_floppy.py`
9. `tools/floppyctl.py`
10. `tests/test_user_onboarding.py`
11. `tests/test_validated_boot_package.py`

No path may be silently added, removed, substituted, renamed, or broadened without new administrator authority. W1 implements zero reusable-product paths.

Expected validated boot-package inventory after later P1 is exactly 61 paths: current 59 plus the accepted-state schema and normative specification.

## 9. Seed and compatibility boundary

No `project-seed/.floppy/*` mutation is required. `.floppy/accepted-state.json` is created only when continuity is lawfully established for an accepted/adopted project; a blank accepted-state seed is forbidden. Frozen V1 lifecycle/authorization schemas and the V2 compatibility-profile schema remain unchanged.

## 10. Future implementation ordering

W1 authorizes none of these later events. A future explicit V2-03 implementation authorization preserves distinct A1 `TR-003-AUTHORIZE-SECTION-IMPLEMENTATION`, B1 `TR-004-START-SECTION-IMPLEMENTATION`, exact eleven-path P1, C1 `TR-005-RECORD-IMPLEMENTATION-COMPLETE`, bounded verification, one later-authorized complete repository suite only after all bounded gates pass, V1 `TR-006-RECORD-VERIFICATION-COMPLETE`, separate administrator result acceptance, separate closeout proposal, and separate closeout application.

## 11. Future verification plan

Required bounded verification after later P1/C1 includes source validation using `D:\A\Tools\Python313\python.exe -B tools\validate_floppy.py . --mode source`; focused tests `tests/test_accepted_state_continuity.py`, `tests/test_user_onboarding.py`, `tests/test_v2_compatibility.py`, `tests/test_validated_boot_package.py`; regression tests `tests/test_tooling.py`, `tests/test_authorization_git_integrity.py`, `tests/test_project_provisioning.py`, `tests/test_final_closure.py`, `tests/test_floppyctl.py`, `tests/test_export_integrity.py`, `tests/test_orchestrator_registry.py`, `tests/test_lifecycle_specification.py`; tracked JSON parsing; and validated boot inventory exactly 61 containing the new schema/spec plus `specs/lifecycle-write-contract.json`.

The complete repository pytest suite may run exactly once only under later explicit authority after P1, C1, and all bounded gates pass. Failure consumes the attempt unless another attempt is separately authorized. W1 runs no complete suite.

## 12. V2-04/V2-05 boundary

V2-03 exposes the accepted-origin/accepted-plan substrate. V2-04 owns paired Continuity Overseer/initial Project Orchestrator bootstrap, durable shared-origin linkage, Continuity Overseer persistence, runtime scope-drift protection, and orchestrator succession. V2-05 owns Official Project Plan binding/finalization, end-to-end paired-bootstrap proof, and final V2 integration/release work.

## 13. Exact W1 control-state scope

1. `.floppy/lifecycle-state.json`
2. `.floppy/manifest.json`
3. `.floppy/orchestrators/Floppy-V2-Development-Orchestrator.md`
4. `.floppy/roadmap/Floppy-V2-Project-Plan.json`
5. `.floppy/work-packages/V2-03.md`

## 14. W1 resulting state

- V2-03 work package: `ACCEPTED_PLANNING_BASELINE`
- V2-03 implementation: `NOT_STARTED`
- V2-03 verification: `NOT_STARTED`
- administrator result acceptance: `PENDING`
- closeout: `NOT_PROPOSED`
- active work authorization: `NONE`
- active implementation authorization: `NONE`
- repository writer: `NONE`
- V2-04: `PLANNED_NOT_AUTHORIZED`
- V2-05: `PLANNED_NOT_AUTHORIZED`
- migration: `NONE`
- main/integration/merge/tag/release: `NOT_AUTHORIZED`

**V2 IMPLEMENTATION AUTHORITY = NONE**

## 15. Next lawful operation

After a validated W1, the next possible lifecycle operation is `TR-003-AUTHORIZE-SECTION-IMPLEMENTATION`. It requires a new, separate, explicit administrator authorization. W1 neither executes nor implies it.

<!-- V2_03_A1_BEGIN -->
# V2-03 A1 implementation authorization

`TR-003-AUTHORIZE-SECTION-IMPLEMENTATION` is applied under explicit administrator authority.

Authorization: `V2_03_IMPLEMENTATION`  
Sole repository writer: `V2_03_WORKING_MODEL`  
Base: `33e3f831303ea25defa41acbbe474b6cc8baff96` / `6ad0bf21373a2cc23a678def44fc4eebd932d368`

Lifecycle: `LC-SECTION-AUTHORIZED-NOT-STARTED`

Implementation remains `NOT_STARTED`; verification remains `NOT_STARTED`; administrator result acceptance remains `PENDING`; closeout remains `NOT_PROPOSED`.

The exact future P1 scope is the eleven reusable-product paths frozen in `.floppy/work-packages/V2-03.md`.

`V2 IMPLEMENTATION AUTHORITY = V2_03_IMPLEMENTATION`
<!-- V2_03_A1_END -->

<!-- V2_03_B1_BEGIN -->
# V2-03 B1 implementation start

`TR-004-START-SECTION-IMPLEMENTATION` is applied. V2-03 implementation is `IN_PROGRESS` under `V2_03_IMPLEMENTATION` / `V2_03_WORKING_MODEL`.

No reusable-product path is changed by B1. P1 remains a separate exact eleven-path commit.
<!-- V2_03_B1_END -->
